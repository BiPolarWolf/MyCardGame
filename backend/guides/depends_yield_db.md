# Как работают get_db, Depends и try-yield-finally в FastAPI

## Введение

В FastAPI эти три концепции работают вместе для создания эффективной системы управления зависимостями, особенно для работы с базами данных. Давай разберём каждую по порядку.

---

## 1. `Depends` - Система внедрения зависимостей

### Что это такое?

`Depends` — это специальный класс FastAPI, который сообщает фреймворку: "Эй, перед выполнением этой функции мне нужно получить результат из другой функции".

### Как это работает?

```python
from fastapi import Depends, FastAPI

app = FastAPI()

def get_current_user():
    return {"user": "John"}

@app.get("/items")
def read_items(user: dict = Depends(get_current_user)):
    return {"user": user, "items": ["item1", "item2"]}
```

**Что происходит:**

1. Когда приходит запрос на `/items`, FastAPI видит `Depends(get_current_user)`
2. FastAPI **сначала** вызывает функцию `get_current_user()`
3. Результат этой функции передаётся в параметр `user`
4. Только **потом** выполняется основная функция `read_items`

### Зачем это нужно?

- ✅ **Переиспользование кода** — одну зависимость можно использовать в разных эндпоинтах
- ✅ **Чистота кода** — логика получения данных отделена от бизнес-логики
- ✅ **Тестирование** — легко подменить зависимость при тестировании

---

## 2. `get_db` - Функция-генератор для работы с БД

### Типичный пример

```python
from sqlalchemy.orm import Session
from database import SessionLocal

def get_db():
    db = SessionLocal()  # Создаём сессию БД
    try:
        yield db  # Отдаём сессию
    finally:
        db.close()  # Закрываем сессию
```

### Что это?

`get_db` — это **функция-генератор** (использует `yield`), которая:
- Создаёт подключение к базе данных
- Предоставляет его твоему эндпоинту
- Гарантирует закрытие подключения после использования

### Использование

```python
@app.get("/users/{user_id}")
def read_user(user_id: int, db: Session = Depends(get_db)):
    user = db.query(User).filter(User.id == user_id).first()
    return user
```

---

## 3. `try-yield-finally` - Механизм гарантированной очистки

### Анатомия конструкции

```python
def get_db():
    db = SessionLocal()          # 1. SETUP - создание ресурса
    try:                         # 2. TRY - защищённый блок
        yield db                 # 3. YIELD - передача ресурса
        # Здесь выполняется код эндпоинта
    finally:                     # 4. FINALLY - гарантированная очистка
        db.close()               # Выполнится ВСЕГДА
```

### Порядок выполнения

Представь такой эндпоинт:

```python
@app.post("/users")
def create_user(user: UserCreate, db: Session = Depends(get_db)):
    new_user = User(name=user.name)
    db.add(new_user)
    db.commit()
    return new_user
```

**Что происходит шаг за шагом:**

```
1. FastAPI видит Depends(get_db)
2. Вызывается get_db()
   ├─ db = SessionLocal()        ← создание сессии
   ├─ try:
   └─ yield db                    ← ПАУЗА! Передаём db в эндпоинт
   
3. Выполняется create_user(...)
   ├─ new_user = User(name=user.name)
   ├─ db.add(new_user)
   └─ db.commit()
   
4. create_user возвращает результат

5. Возвращаемся в get_db()
   └─ finally:
      └─ db.close()              ← закрытие сессии (ВСЕГДА!)
```

### Почему `finally` так важен?

**Сценарий без `finally`:**

```python
def get_db():
    db = SessionLocal()
    return db  # ❌ Сессия никогда не закроется!
```

**С `finally`:**

```python
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()  # ✅ Закроется ВСЕГДА, даже при ошибке
```

**Что, если произошла ошибка?**

```python
@app.post("/users")
def create_user(user: UserCreate, db: Session = Depends(get_db)):
    new_user = User(name=user.name)
    db.add(new_user)
    raise Exception("Что-то пошло не так!")  # ❌ ОШИБКА!
    db.commit()  # Не выполнится
```

**Даже при ошибке:**
1. Эндпоинт выбросит исключение
2. FastAPI поймает ошибку
3. **Но сначала** выполнится `finally: db.close()`
4. Только потом ошибка вернётся клиенту

---

## Полный пример из реальной жизни

```python
from fastapi import FastAPI, Depends, HTTPException
from sqlalchemy.orm import Session
from database import SessionLocal
import models

app = FastAPI()

# Функция-генератор для управления сессией БД
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# Эндпоинт с зависимостью
@app.get("/users/{user_id}")
def get_user(user_id: int, db: Session = Depends(get_db)):
    user = db.query(models.User).filter(models.User.id == user_id).first()
    
    if user is None:
        raise HTTPException(status_code=404, detail="User not found")
    
    return user

# Эндпоинт с созданием пользователя
@app.post("/users")
def create_user(name: str, db: Session = Depends(get_db)):
    new_user = models.User(name=name)
    db.add(new_user)
    db.commit()
    db.refresh(new_user)
    return new_user
```

---

## Визуализация процесса

```
HTTP Request → FastAPI
                  ↓
              [Depends(get_db)]
                  ↓
            get_db() вызывается
                  ↓
        db = SessionLocal() ← создание
                  ↓
            try: yield db ← передача
                  ↓
        ┌─────────────────────┐
        │   Твой эндпоинт     │
        │  (работает с db)    │
        └─────────────────────┘
                  ↓
         finally: db.close() ← очистка
                  ↓
            Response → Client
```

---

## Ключевые моменты

### ✅ Что нужно запомнить

1. **`Depends`** — это "подтяни зависимость перед выполнением функции"
2. **`yield`** — это "останови выполнение здесь, отдай значение, и продолжи потом"
3. **`finally`** — это "выполни этот код ОБЯЗАТЕЛЬНО, что бы ни случилось"

### 💡 Почему это мощная комбинация?

- **Автоматическое управление ресурсами** — не нужно думать о закрытии соединений
- **Защита от утечек** — даже при ошибках ресурсы освобождаются
- **Чистый код** — логика работы с БД отделена от бизнес-логики
- **Легкое тестирование** — можно подменить `get_db` на mock-версию

---

## Дополнительные примеры

### Несколько зависимостей

```python
def get_current_user(token: str = Depends(oauth2_scheme)):
    return decode_token(token)

@app.get("/items")
def read_items(
    db: Session = Depends(get_db),
    user: User = Depends(get_current_user)
):
    return db.query(Item).filter(Item.owner_id == user.id).all()
```

### Зависимости с параметрами

```python
def get_db_with_timeout(timeout: int = 30):
    db = SessionLocal()
    db.execute(f"SET statement_timeout = {timeout * 1000}")
    try:
        yield db
    finally:
        db.close()

@app.get("/slow-query")
def slow_query(db: Session = Depends(lambda: get_db_with_timeout(60))):
    return db.query(BigTable).all()
```

---

## Заключение

Связка `Depends` + `get_db` + `try-yield-finally` — это стандартный паттерн в FastAPI для работы с базами данных. Он обеспечивает:

- 🔒 Безопасное управление ресурсами
- 🧹 Автоматическую очистку
- 📦 Переиспользуемость кода
- 🎯 Чистую архитектуру

Теперь ты знаешь, как это работает под капотом! 🚀
